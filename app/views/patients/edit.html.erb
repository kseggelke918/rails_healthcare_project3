This is the user edit page 

<%= form_for @patient, method: :patch, url: user_patient_path(current_user, @patient) do |f| %>
    <%= f.label "Patient Name:"%>
    <%= f.text_field :name %><br>
    <%= f.label "Patient Symptoms:"%>
    <%= f.text_area :symptoms%><br>
    <br>
    <%= f.submit "Submit"%>
<% end %>

<%= link_to "Delete Patient", delete_patient_path, method: :delete, data: { confirm: "Are you Sure?" } %>

# be able to add doctor on form